.. highlight:: shell

============
Installation
============


Stable release
--------------

To install shazam API access, run this command in your terminal:

.. code-block:: console

    $ pip install shazam_api

This is the preferred method to install shazam API access, as it will always install the most recent stable release.

If you don't have `pip`_ installed, this `Python installation guide`_ can guide
you through the process.

.. _pip: https://pip.pypa.io
.. _Python installation guide: http://docs.python-guide.org/en/latest/starting/installation/


From sources
------------

The sources for shazam API access can be downloaded from the `Github repo`_.

You can either clone the public repository:

.. code-block:: console

    $ git clone git://github.com/ghxm/shazam_api

Or download the `tarball`_:

.. code-block:: console

    $ curl -OJL https://github.com/ghxm/shazam_api/tarball/master

Once you have a copy of the source, you can install it with:

.. code-block:: console

    $ python setup.py install


.. _Github repo: https://github.com/ghxm/shazam_api
.. _tarball: https://github.com/ghxm/shazam_api/tarball/master
